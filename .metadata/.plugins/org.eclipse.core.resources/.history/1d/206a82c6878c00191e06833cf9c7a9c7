import java.io.IOException;
import javax.servlet.Filter;
import javax.servlet.FilterChain;
import javax.servlet.FilterConfig;
import javax.servlet.ServletException;
import javax.servlet.ServletRequest;
import javax.servlet.ServletResponse;
import javax.servlet.http.HttpServletRequest;

public class DataFilter implements Filter {

	double limite = 0.5;

	public DataFilter() {
	}

	public void destroy() {
	}

	public void doFilter(ServletRequest request, ServletResponse response, FilterChain chain) throws IOException, ServletException {
		int valorAleatorio = (int) (Math.random() * 30);

		// Alterar antes
		//if (valorAleatorio > limite)
		System.out.println("1: " + ((HttpServletRequest) request).getServletContext().getAttribute("mensagem"));
		((HttpServletRequest) request).getServletContext().setAttribute("mensagem", "A diferença entre as datas é (alterado): " + valorAleatorio + " dia(s).");
		System.out.println("2: " + ((HttpServletRequest) request).getServletContext().getAttribute("mensagem"));

		chain.doFilter(request, response);

		System.out.println("3: " + ((HttpServletRequest) request).getServletContext().getAttribute("mensagem"));
		((HttpServletRequest) request).getServletContext().setAttribute("mensagem", "A diferença entre as datas é (alterado): " + valorAleatorio + " dia(s).");
		System.out.println("4: " + ((HttpServletRequest) request).getServletContext().getAttribute("mensagem"));
	}

	public void init(FilterConfig fConfig) throws ServletException {
		try {
			limite = Double.parseDouble(fConfig.getInitParameter("limite"));
		} catch (NumberFormatException numberFormatException) {
			// limite = 0.5;
		} catch (Exception exception) {
			// limite = 0.5;
		}
	}
}